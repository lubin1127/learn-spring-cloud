server:
  port: 80

spring:
  application:
    name: cloud-order-service
  cloud:
    consul:
      host: 192.168.32.129
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        prefer-ip-address: true